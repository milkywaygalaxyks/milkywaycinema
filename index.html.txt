<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Milkyway Cinema</title>
  <style>
    body {
      margin: 0;
      font-family: Arial;
      background: #0d1b1e;
      color: white;
      cursor: none; 
      overflow-x: hidden;
    }

    .welcome-screen {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: #0d1b1e;
      z-index: 9999;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      animation: fadeOut 2s ease forwards;
      animation-delay: 2s; 
      overflow: hidden;
    }

    .welcome-screen::before {
      content: "";
      position: absolute;
      width: 100%; height: 100%;
      background: url('https://www.transparenttextures.com/patterns/stardust.png');
      opacity: 0.2;
      animation: twinkle 3s infinite ease-in-out;
    }

    @keyframes twinkle {
      0%, 100% { opacity: 0.2; }
      50% { opacity: 0.4; }
    }

    .welcome-text {
      font-size: 32px;
      font-weight: bold;
      color: white;
      text-shadow: 0 0 12px #20c997, 0 0 24px #17a589;
      animation: pulseText 2s infinite ease-in-out;
      z-index: 1;
    }

    @keyframes pulseText {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.6; transform: scale(1.05); }
    }

    .spinner {
      width: 80px;
      height: 80px;
      border: 6px solid #fff;
      border-top: 6px solid #20c997;
      border-radius: 50%;
      animation: spin 2s linear infinite;
      box-shadow: 0 0 20px #20c997;
      margin-top: 20px;
      z-index: 1;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    @keyframes fadeOut {
      to { opacity: 0; visibility: hidden; display: none; }
    }

    header {
      background: #20c997;
      padding: 20px;
      font-size: 24px;
      font-weight: bold;
      text-align: center;
      text-shadow: 0 0 12px #20c997, 0 0 24px #17a589;
    }

    .search-container {
      text-align: center;
      margin: 20px;
    }

    .search-container input {
      width: 60%;
      max-width: 400px;
      padding: 10px;
      border-radius: 25px;
      border: none;
      font-size: 16px;
      outline: none;
      background: #122426;
      color: white;
      border: 2px solid #20c997;
      caret-color: transparent; 
    }

    .search-container input::placeholder {
      color: #aaa;
    }

    .movie-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      padding: 20px;
    }

    .movie-card {
      margin: 15px;
      background: #122426;
      border-radius: 10px;
      overflow: hidden;
      width: 200px;
      cursor: pointer;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      text-align: center;
      border: 1px solid #20c997;
      outline: none; 
    }

    .movie-card:hover, .movie-card:focus {
      transform: scale(1.08);
      box-shadow: 0 8px 25px rgba(32,201,151,0.8);
      outline: 5px solid white;
      z-index: 10;
    }

    .movie-card img {
      width: 100%;
      height: 280px;
      object-fit: cover;
    }

    .movie-card h3 {
      margin: 0;
      padding: 12px;
      font-size: 16px;
      background: #20c997;
      color: white;
      font-weight: bold;
    }
  </style>
</head>
<body>
  
  <!-- 🖱️ THE BULLETPROOF TV CURSOR IS RIGHT HERE -->
  <div id="tv-cursor" style="position: fixed; width: 35px; height: 35px; background-color: #20c997; border: 4px solid white; border-radius: 50%; pointer-events: none; z-index: 10000; box-shadow: 0 0 20px #20c997; display: block; left: 50%; top: 50%;"></div>

  <div class="welcome-screen">
    <div class="welcome-text">Welcome to Milkyway Cinema</div>
    <div class="spinner"></div>
  </div>

  <header>🎬 Milkyway Cinema</header>

  <div class="search-container">
    <input type="text" id="searchInput" placeholder="🔍 Search for a movie..." tabindex="0">
  </div>

  <div class="movie-container" id="movieContainer"></div>

  <script>
    function convertDriveLink(link) {
      var match = link.match(/\/d\/(.+?)\//);
      return match ? "https://drive.google.com/file/d/" + match[1] + "/preview" : link;
    }

    var movies = [
      { id: 1, title: "Mahavatar Narsimha (2025)", poster: "Posters/Poster1.jpg", video: convertDriveLink("https://drive.google.com/file/d/1nV9TCnYVA4M9JqzxX2mM2sgfQlDV6R2o/view?usp=sharing") },
      { id: 2, title: "Kudumbasthan (2025)", poster: "Posters/Poster2.jpg", video: convertDriveLink("https://drive.google.com/file/d/1P-UPst7OvSKGkdDTNIAAUpCQsxg71i12/view?usp=sharing") },
      { id: 3, title: "SIR (2024)", poster: "Posters/Poster3.jpg", video: convertDriveLink("https://drive.google.com/file/d/1XHZh-oNHQriNF0H0M9jfA4XoVZuSEpIE/view?usp=sharing") },
      { id: 4, title: "3BHK (2025)", poster: "Posters/Poster4.jpg", video: convertDriveLink("https://drive.google.com/file/d/16vrFBJ3UbtT2e4ht-lf2QBpoVGqbF8zX/view?usp=sharing") },
      { id: 5, title: "Hridayam (2022)", poster: "Posters/Poster5.jpg", video: "https://youtu.be/fJftRAwst3w" },
      { id: 6, title: "Kingston (2025)", poster: "Posters/Poster6.jpg", video: convertDriveLink("https://drive.google.com/file/d/13yKXv-aVe_UbVpUb1uvOmpFav69ANKob/view?usp=sharing") },
      { id: 7, title: "Idly Kadai (2025)", poster: "Posters/Poster7.jpg", video: convertDriveLink("https://drive.google.com/file/d/1lnpKx3YsEdQV8MfKBjvrrGCKln7U6rFV/view?usp=sharing") }
    ];

    localStorage.setItem("movies", JSON.stringify(movies));

    var container = document.getElementById("movieContainer");
    var searchInput = document.getElementById("searchInput");

    function displayMovies(filter) {
      if (!filter) filter = "";
      container.innerHTML = "";
      
      for (var i = 0; i < movies.length; i++) {
        var movie = movies[i];
        if (movie.title.toLowerCase().indexOf(filter.toLowerCase()) > -1) {
          
          var card = document.createElement("div");
          card.className = "movie-card";
          card.tabIndex = 0; 
          
          // Use closure to lock in the correct movie ID
          (function(id) {
            card.innerHTML = "<img src='" + movie.poster + "' alt='" + movie.title + "'><h3>" + movie.title + "</h3>";
            
            card.onclick = function() {
              window.location.href = "movie.html?id=" + id;
            };
            
            card.addEventListener("keydown", function(event) {
              var code = event.keyCode || event.which;
              if (code === 13) {
                window.location.href = "movie.html?id=" + id;
              }
            });
          })(movie.id);

          container.appendChild(card);
        }
      }
    }

    displayMovies();

    searchInput.addEventListener("input", function(e) {
      displayMovies(e.target.value);
    });

    // --- 🖱️ VIRTUAL TV MOUSE CURSOR LOGIC ---
    window.onload = function() {
        var cursor = document.getElementById("tv-cursor");
        
        var cx = window.innerWidth / 2;
        var cy = window.innerHeight / 2;
        var cursorSpeed = 20; 
        var keysPressed = {};

        function updateCursor() {
            cursor.style.left = cx + "px";
            cursor.style.top = cy + "px";
        }
        updateCursor();

        window.addEventListener("keydown", function(e) {
            var code = e.keyCode || e.which;
            keysPressed[code] = true;

            if (code === 13) { // 13 is OK/Enter
                cursor.style.display = 'none';
                var elementBelow = document.elementFromPoint(cx + 17, cy + 17);
                cursor.style.display = 'block';

                if (elementBelow) {
                    elementBelow.click();
                    elementBelow.focus(); 
                }
            }
        });

        window.addEventListener("keyup", function(e) {
            var code = e.keyCode || e.which;
            keysPressed[code] = false;
        });

        function moveCursorLoop() {
            var moved = false;

            if (keysPressed[38]) { cy -= cursorSpeed; moved = true; } // Up
            if (keysPressed[40]) { cy += cursorSpeed; moved = true; } // Down
            if (keysPressed[37]) { cx -= cursorSpeed; moved = true; } // Left
            if (keysPressed[39]) { cx += cursorSpeed; moved = true; } // Right

            if (moved) {
                cx = Math.max(0, Math.min(window.innerWidth - 35, cx));
                cy = Math.max(0, Math.min(window.innerHeight - 35, cy));
                
                if (cy > window.innerHeight - 80) window.scrollBy(0, cursorSpeed);
                if (cy < 80) window.scrollBy(0, -cursorSpeed);

                updateCursor();
            }
            
            if (window.requestAnimationFrame) {
                requestAnimationFrame(moveCursorLoop);
            } else {
                setTimeout(moveCursorLoop, 16);
            }
        }
        moveCursorLoop();
    };
  </script>
</body>
</html>
